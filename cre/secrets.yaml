# ==========================================================================
# CRE SECRETS FILE
# ==========================================================================
# The adjudicator needs no secrets. Everything it reads is either public
# chain state or evidence whose integrity is proved by the seller's
# signature, so there is nothing here for the Vault DON to release.
#
# If a future SLA needs an authenticated source (a paid data feed, say), map
# the secret id to an environment variable here and fetch it inside the
# enclave with runtime.getSecret({ id }).
secretsNames: {}
